function loadProfileProgress(el){
    var url = '/doctor/snippets/profile_progress';
    el.load(url);
}

function loadProfileProgressDetail(el){
    var url = '/doctor/snippets/profile_progress_detail';
    el.load(url);
}